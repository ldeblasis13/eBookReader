import XCTest
import GRDB
@testable import EBookReader

final class BookRepositoryTests: XCTestCase {

    func testPlaceholder() throws {
        // Placeholder test — will be expanded as the database layer matures
        XCTAssertTrue(true)
    }
}
